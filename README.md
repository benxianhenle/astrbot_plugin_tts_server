# AstrBot TTS 服务器插件

通过 API 调用远程 TTS 服务器进行语音合成，支持角色选择和参考音频切换。

***

## 网站地址

**TTS 服务平台**: <https://benxianhenl.cn/>

***

## ⚠️ 重要提示

**本插件与 AstrBot 的「自动分段」功能有冲突**，使用时请务必在 AstrBot 设置中**关闭自动分段**，否则可能导致语音合成异常或失败。

***

## 完整使用流程

### 第一步：注册账户并登录

1. 打开网站 <https://benxianhenl.cn/>
2. 点击右上角的 **"注册"** 按钮
3. 填写手机号和密码完成注册
4. 注册成功后自动登录，或使用已有账号登录

!\[注册页面截图]\(images/注册.png null)

***

### 第二步：创建角色并添加模型

1. 登录后进入 **"控制台"** 页面
2. 点击 **"创建角色"** 按钮
3. 填写角色名称和描述
4. 选择模型版本（GPT 模型和 SoVITS 模型）
5. 点击创建完成角色

!\[创建角色0]\(images/创建角色0.png null)
!\[创建角色1]\(images/创建角色1.png null)
!\[创建角色2]\(images/创建角色2.png null)

***

### 第三步：等待模型校验完成

上传模型后，系统会自动进行 GPU 验证，请耐心等待模型状态变为 **"模型可用"**。

!\[等待模型上传完成]\(images/等待模型上传完成.png null)
!\[模型上传完成弹窗]\(images/模型上传完成弹窗.png null)
!\[模型状态查看]\(images/模型状态查看.png null)

***

### 第四步：配置参考音频

1. 进入角色的 **"参考音频"** 标签页
2. 点击 **"上传参考音频"** 按钮
3. 选择音频文件（支持 wav 格式）
4. 填写音频名称和对应的文本内容

!\[添加参考音频1]\(images/添加参考音频1.png null)
!\[上传音频3]\(images/上传音频3.png null)
!\[上传音频完成弹窗]\(images/上传音频完成弹窗.png null)
!\[查看参考音频状态]\(images/查看参考音频状态.png null)

***

### 第五步：在 Web 页面测试推理

在网站控制台直接测试语音合成，确保模型和参考音频正常工作。

!\[可以在web中尝试推理]\(images/可以在web中尝试推理.png null)
!\[推理结果]\(images/推理结果.png null)

***

### 第六步：获取 API Key

1. 点击右上角用户头像，选择 **"个人中心"**
2. 进入 **"API Key 管理"** 标签页
3. 点击 **"创建 API Key"** 按钮
4. 填写名称（如 "AstrBot"），点击确认
5. **复制生成的 API Key**（注意：只显示一次！）

!\[API Key 创建1]\(images/key的创建1.png null)
!\[API Key 创建2]\(images/key的创建2.png null)
!\[API Key 创建3]\(images/key的创建3.png null)

***

### 第七步：配置 AstrBot 插件

#### 7.1 安装插件

**方式一：手动安装（推荐）**

1. 将本插件文件夹 `astrbot_plugin_tts_server` 复制到 AstrBot 的 `data/plugins/` 目录
2. 重启 AstrBot

**方式二：通过 AstrBot 插件管理界面安装**

1. 打开 AstrBot 管理面板
2. 进入 **"插件管理"** → 点击 **"安装插件"** 按钮
3. 选择 **"从链接安装"** 选项
4. 输入 GitHub 仓库地址：`https://github.com/benxianhenl/astrbot_plugin_tts_server.git`
5. 点击安装，等待安装完成

!\[安装插件]\(images/安装插件.png null)

#### 7.2 配置插件

1. 打开 AstrBot 管理面板
2. 进入 **"插件管理"** → 找到 **"TTS 服务器插件"**
3. 点击 **"配置"** 按钮

!\[粘贴 API Key]\(images/粘贴key.png null)

#### 7.3 填写配置项

| 配置项                               | 说明             | 示例值                                              |
| --------------------------------- | -------------- | ------------------------------------------------ |
| **client.base\_url**              | TTS 服务器 API 地址 | `https://benxianhenl.cn/api/proxy`               |
| **client.api\_key**               | 从网站获取的 API Key | `your-api-key-here`                              |
| **client.timeout**                | 请求超时时间（秒）      | `60`                                             |
| **default\_params.role**          | 默认角色名称         | `霄宫`                                             |
| **default\_params.reference**     | 默认参考音频文件名      | `【默认】哇，你做点心的手艺很不一般啊！去祭典上摆摊的话，肯定会成为最热门的那一个吧！.wav` |
| **default\_params.language**      | 默认语言           | `zh`（中文）                                         |
| **default\_params.speed\_factor** | 默认语速           | `1.0`                                            |

#### 7.4 保存配置

点击 **"保存"** 按钮，插件会自动加载配置。

***

### 第八步：重载插件并刷新角色信息

1. 在插件管理页面，点击 **"重载"** 按钮刷新插件配置
2. 插件会自动从服务器获取可用的角色列表

!\[重载插件刷新配置]\(images/重载插件刷新配置.png null)

***

### 第九步：配置角色并开始使用

1. 在 AstrBot 中配置你的机器人使用本插件
2. 选择要使用的角色和参考音频
3. **重要：关闭 AstrBot 的「自动分段」功能**

!\[选择完成角色既可]\(images/选择完成角色既可.png null)

配置完成后，你就可以正常使用 TTS 服务器插件了！

***

## 插件命令

安装并配置完成后，可以使用以下命令：

| 命令            | 说明        | 示例         |
| ------------- | --------- | ---------- |
| `/说 <文本>`     | 直接合成语音    | `/说 你好世界`  |
| `/角色列表`       | 显示可用角色    | `/角色列表`    |
| `/参考音频 <角色名>` | 显示角色的参考音频 | `/参考音频 霄宫` |
| `/TTS缓存`      | 显示缓存统计    | `/TTS缓存`   |
| `/清除TTS缓存`    | 清除所有缓存    | `/清除TTS缓存` |

***

## 情绪配置（高级）

可以在插件配置中添加多套情绪参数，通过触发词自动匹配：

```json
{
  "name": "开心",
  "keywords": ["开心", "高兴", "哈哈"],
  "role": "霄宫",
  "reference": "开心.wav",
  "speed_factor": 1.2
}
```

当 AI 回复包含 "开心"、"高兴" 或 "哈哈" 时，会自动使用这套情绪参数。

!\[参考音频2]\(images/参考音频2.png null)

***

## 自动语音转换

插件支持按概率自动将 LLM 回复转为语音：

- **only\_llm\_result**: 只处理 LLM 返回的消息
- **tts\_prob**: 自动转语音的概率（0-1）
- **max\_msg\_len**: 最大文本长度限制

***

## 常见问题

### Q: 提示 "API Key 无效"？

A: 请检查：

1. API Key 是否复制完整（不要有多余空格）
2. API Key 是否已启用（在个人中心查看状态）
3. base\_url 是否正确（应为 `https://benxianhenl.cn/api/proxy`）

### Q: 提示 "角色不存在"？

A: 请检查：

1. 角色名称是否与网站上创建的一致（区分大小写）
2. 使用 `/角色列表` 命令查看可用角色

### Q: 提示 "参考音频不存在"？

A: 请检查：

1. 参考音频文件名是否完整（包含扩展名）
2. 使用 `/参考音频 <角色名>` 命令查看可用参考音频

### Q: 语音合成很慢？

A: 首次合成需要加载模型，会比较慢。后续相同参数的合成都走缓存，速度会很快。

### Q: 语音合成失败或异常？

A: **请检查是否关闭了 AstrBot 的「自动分段」功能**，这是最常见的冲突问题。

### Q: 如何刷新角色列表？

A: 重载插件即可自动刷新角色信息。

### Q: GitHub 仓库地址是什么？为什么克隆失败？

A: 正确的 GitHub 仓库地址是：

```
https://github.com/benxianhenl/astrbot_plugin_tts_server.git
```

注意是 `benxianhenl`（没有多余的 `e`）。如果使用错误的地址 `https://github.com/benxianhenle/astrbot_plugin_tts_server.git` 会提示仓库不存在。

***

## 参与贡献

欢迎贡献代码、报告问题或提出改进建议！

- **报告问题**：请在 [GitHub Issues](https://github.com/benxianhenl/astrbot_plugin_tts_server/issues) 中提交
- **贡献代码**：请阅读 [CONTRIBUTING.md](CONTRIBUTING.md) 了解详细流程
- **行为准则**：请遵守 [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)

***

## 技术支持

- **网站**: <https://benxianhenl.cn/>
- **GitHub**: <https://github.com/benxianhenl/astrbot_plugin_tts_server>
- **问题反馈**: 请在 GitHub Issues 中提交

***

## 许可证

本项目采用 MIT 许可证。详情请见 [LICENSE](LICENSE) 文件。

```text
MIT License

Copyright (c) 2024 benxianhenl

特此免费授予任何获得本软件及相关文档文件（以下简称"软件"）副本的人，
不受限制地处理本软件，包括但不限于使用、复制、修改、合并、发布、分发、再许可和/或销售本软件的副本，
并允许向其提供本软件的人员这样做，但须满足以下条件：

上述版权声明和本许可声明应包含在本软件的所有副本或主要部分中。

本软件按"原样"提供，不提供任何形式的明示或暗示保证，包括但不限于对适销性、
特定用途适用性和非侵权性的保证。在任何情况下，作者或版权持有人均不对
因本软件或本软件的使用或其他交易而引起的任何索赔、损害赔偿或其他责任承担责任，
无论是在合同诉讼、侵权诉讼还是其他诉讼中。
```
